import {PLUS,MINUS} from './Actiontype';

const Reducer = (state = 0, action) =>
{
    // switch(action.Type)
    // {
    //     case PLUS:
    //         return state + value;
    //     case MINUS:
    //         return state - value;

    // }

}

module.exports = Reducer;